
How to Use

Once the command runs, a web browser tab will open automatically at http://localhost:8501.

Type any prompt, question, or customer support query into the input bar at the bottom.

The virtual agent will process the natural language context and stream back an intuitive, human-like response immediately.
